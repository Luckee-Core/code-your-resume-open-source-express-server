# Changelog

## 0.1.0 — 2026-09-24

Initial open-source release.

- Express CRM API on port 3053 with `/api/data` entity routers and studio routes
- Supabase DDL runbook in `docs/` (CRM, studios, generation ledgers)
- Wire contract in the companion web repo; `GET /api/health` and `GET /api-docs.json`
- MIT license, CONTRIBUTING, SECURITY, and CI workflow
