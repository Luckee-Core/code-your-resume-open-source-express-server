# Railway/Nixpacks: Node 22+ (Supabase client needs global WebSocket).
[variables]
NIXPACKS_NODE_VERSION = "22"
NODE_ENV = "production"

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/index.js"
